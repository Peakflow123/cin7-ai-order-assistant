# NexOrder AI Branding + Input Channels Pack

This pack changes:

1. App name to **NexOrder AI**.
2. Header subtitle now shows the logged-in client's company name instead of Nexvista Consulting.
3. Admin link is only visible for platform admin users.
4. Dashboard client view uses the client/company name.
5. Input Channels page added for:
   - Microsoft Outlook
   - Gmail
   - WhatsApp
6. Existing manual pasted order test remains available.

## Copy files
Copy everything from this ZIP into your existing project and replace files.

## Push
```cmd
cd C:\Users\Dell\Downloads\cin7-ai-order-assistant
git add .
git commit -m "Brand app as NexOrder AI and add input channels"
git push
```
