'use client';
import { useState } from 'react';
export default function Settings(){const[accountId,setAccountId]=useState('');const[apiKey,setApiKey]=useState('');const[msg,setMsg]=useState('');async function save(){const r=await fetch('/api/cin7/save',{method:'POST',body:JSON.stringify({accountId,apiKey})});setMsg(await r.text());}async function sync(){setMsg('Syncing...');const r=await fetch('/api/cin7/sync',{method:'POST'});setMsg(await r.text());}return <main className="p-6 max-w-3xl mx-auto"><div className="card space-y-3"><h1 className="text-2xl font-bold">Cin7 Connection</h1><input className="input" placeholder="Cin7 Account ID" onChange={e=>setAccountId(e.target.value)}/><input className="input" placeholder="Cin7 API Key" onChange={e=>setApiKey(e.target.value)}/><div className="flex gap-3"><button className="btn" onClick={save}>Save</button><button className="btn" onClick={sync}>Sync products/customers</button></div><p>{msg}</p></div></main>}
