import { NextResponse } from 'next/server';import { prisma } from '@/lib/db';import bcrypt from 'bcryptjs';import { signSession } from '@/lib/auth';
export async function POST(req:Request){const b=await req.json();const company=await prisma.company.create({data:{name:b.companyName}});const user=await prisma.user.create({data:{email:b.email,name:b.name,passwordHash:await bcrypt.hash(b.password,10),companyId:company.id}});const res=new NextResponse('ok');res.cookies.set('session',signSession({userId:user.id,companyId:company.id,email:user.email}),{httpOnly:true,path:'/'});return res;}
