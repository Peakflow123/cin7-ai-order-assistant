import Link from 'next/link';import { requireSession } from '@/lib/auth';import { prisma } from '@/lib/db';
export default async function Orders(){const s=await requireSession();const orders=await prisma.order.findMany({where:{companyId:s.companyId},orderBy:{createdAt:'desc'},take:50});return <main className="p-6 max-w-6xl mx-auto"><h1 className="text-3xl font-bold mb-4">Orders</h1><div className="space-y-3">{orders.map(o=><Link key={o.id} href={'/orders/'+o.id} className="card block hover:bg-slate-50"><b>{o.subject||o.poNumber||'Order'}</b><p>{o.sender} - {o.status}</p></Link>)}</div></main>}
